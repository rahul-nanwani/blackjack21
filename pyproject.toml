[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "blackjack21"
authors = [{name = "Rahul Nanwani", email = "rahulnanwani@icloud.com"}]
dynamic = ["version", "description"]
